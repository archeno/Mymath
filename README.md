## cmake 示例项目
 本项目作为基于cmake构建系统的示例项目。
## 使用到的技术点
### CMake关键语法
- cmake 现代用法-基于对象传递依赖
- 创建库`add_library`
- 引用外部库 `find_package`
### 配置 编译
- 配置： `make config`
- 编译： `make build`
- 清理   `make clean`
### Github Workflow
创建workflow 当执行push 时自动执行编译运行
